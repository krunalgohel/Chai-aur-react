function Chai() {
    return(
        <>
            <hr></hr>
            <h4>Notes:</h4>
            <p><strong>1.</strong> for insert or write any variable into our <b>JSX/HTML</b> tag use "<b style={{color:"green"}}>{ }</b> (curly braces) inside this you can write any variable."</p>
            <p><strong>2.</strong> for example we define one variable inside function "<b style={{color:"green"}}>Const userName = "chai aur react"</b>" now we can print this value as follow "<b style={{color:"green"}}>&lt;h1&gt;Hello <span style={{color:"blue"}}>&#123;userName&#125;</span>/h1&gt;</b>". that will give you this print in output <b>Hello chai aur react</b>.</p>
            <p><strong>3.</strong> <b style={{color:"green"}}>Babel</b> transpiles modern JavaScript for use in React components and all browsers.</p>
        </>
    )
}

export default Chai